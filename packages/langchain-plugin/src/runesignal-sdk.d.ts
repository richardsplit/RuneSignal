/**
 * Minimal type stubs for @runesignal/sdk peer dependency.
 * Replace with the real package once published to npm.
 */
declare module '@runesignal/sdk' {
  export type BlastRadius = 'low' | 'medium' | 'high' | 'critical';

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  export type TrustLayer = any;
}
